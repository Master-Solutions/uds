import React from 'react';

const ShellLayoutMainContainer = ({children}) => {
  return (
    <div className="shell-main">
      {children}
    </div>
  )
};

export default ShellLayoutMainContainer;
